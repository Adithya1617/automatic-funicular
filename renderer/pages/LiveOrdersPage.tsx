import { Placeholder } from '@renderer/components/Placeholder';

export function LiveOrdersPage() {
  return <Placeholder name="Live orders" description="Slice 4 will wire mock channel adapters and the live feed." />;
}
